manifestVersion: subsquid.io/v0.1
name: showcase04-all-erc20-mints
version: 1
description: |-
  The very first evm squid from manifest
build:

deploy:
  addons:
    postgres:
  processor:
    cmd: [ "sqd", "process:prod" ]
  api:
    cmd: [ "sqd", "serve:prod" ]
